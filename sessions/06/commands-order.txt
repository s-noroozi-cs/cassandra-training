1. start cluster

    $ docker compose -f docker-compose.yml up

2. wait and verify start 

    $ for f in $(echo "1 2 3"); \
    do \
        echo "======== cassandra-node-$f health check==========="; \
        docker exec -it cassandra-node-$f nodetool status; \
    done;

